RikiPDYea is the version that is modified to be executed from Ubuntu server.
You can use PyCharm and command line tools to start the Flask/Wiki system with “python RikiPDYea.py”.

## Configuration
1. Always use virtualenv and pip.
    * pip install -r requirements.txt
2. When you want to use login, make PRIVATE = True in config.py.

